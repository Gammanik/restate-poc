server.port=8081
spring.application.name=restate-loan-poc

# Restate Configuration
# Restate runtime endpoint (where services are registered)
restate.endpoint.url=http://localhost:8080

# Logging
logging.level.org.example=INFO
logging.level.dev.restate=DEBUG
